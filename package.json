{
  "name": "my-blog",
  "private": true,
  "version": "0.0.0",
  "description": "pnpm monorepo workspace for my-blog",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "dev": "pnpm -r --filter ./apps/* run dev"
  }
}
